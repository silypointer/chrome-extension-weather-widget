# Weather Widget Chrome Extension - Environment Variables Example
# Note: Chrome extensions cannot directly use .env files, but this shows the structure
# Copy this to .env and fill in your actual values for development

# OpenWeatherMap API Configuration
OPENWEATHER_API_KEY=your_actual_api_key_here
OPENWEATHER_BASE_URL=https://api.openweathermap.org/api
OPENWEATHER_UNITS=metric
OPENWEATHER_LANGUAGE=en

# Extension Configuration
EXTENSION_NAME=Weather Widget
EXTENSION_VERSION=1.0.0
